services:
  - type: web
    name: perfcnc
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    envVars:
      - key: USE_CLAUDE_API
        value: "False"
      - key: PYTHON_VERSION
        value: "3.11.0"
